{% extends 'base.html.twig' %}

{% block title %}EffetsSecondaire{% endblock %}

{% block body %}
    <h1>EffetsSecondaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ effets_secondaire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ effets_secondaire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_effets_secondaire_index') }}">back to list</a>

    <a href="{{ path('app_effets_secondaire_edit', {'id': effets_secondaire.id}) }}">edit</a>

    {{ include('effets_secondaire/_delete_form.html.twig') }}
{% endblock %}
